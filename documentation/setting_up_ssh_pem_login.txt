# First generate the file :
ssh-keygen -t rsa -b 2048 -v

# Rename the private file to have .pem

# Send the file to the server
ssh-copy-id -f -i <file_path> "user@ip" -p <port>

# Login with
ssh -i <pem_file_path> "user@ip" -p <port>